spring:
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 20

logging:
  level:
    root: WARN
    com.factusimple: INFO

# Swagger desactivado en producción.
springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false
