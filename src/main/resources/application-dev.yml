spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.factusimple: DEBUG
    org.springframework.web: DEBUG

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true
